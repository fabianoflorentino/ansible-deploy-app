---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: {{ api_name }}
  name: {{ api_name }}
  namespace: qa
spec:
  externalTrafficPolicy: Cluster
  ports:
  - nodePort: {{ nodeport }}
    port: {{ api_port }}
    protocol: TCP
    targetPort: {{ api_port }}
  selector:
    app: {{ api_name }}
  sessionAffinity: None
  type: NodePort